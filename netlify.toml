[build]
  base = "docs"
  command = "bundle exec jekyll build"
  publish = "_site"
  # Skip when website files are unchanged
  ignore = "git diff --quiet $CACHED_COMMIT_REF $COMMIT_REF -- docs/ netlify.toml"

[build.environment]
  JEKYLL_ENV = "production"
  RUBY_VERSION = "3.3"
